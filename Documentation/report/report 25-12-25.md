# Kịch bản đã thống nhất trc đó
backgr: SV -> gw insync
ESP32 send req -> GW (add prev updated date) 
GW send val_req -> SV 
SV send val_res -> GW
GW send status -> ESP32 (firmwareLIst -> GW fixed 1st)
ESP32 pick firmware -> GW (required most, recent, need to update, based on previous updated date) (picking strategy) 
GW check on GW -> ESP32
ESP32 download firmware -> GW
if not found -> GW check on SV -> GW (registrate FW for redirect to foundFWscript) -> ESP32
SV send fw -> GW -> ESP32
ESP32 update firmware

# Hoàn thành
done base script where FW store on GW and ESP32 can pick and download FW from GW
done script where FW not found on GW, GW check on SV, GW login SW to get SW, GW send FW to ESP32
k lam phan SV thong bao FW moi vi ESP32 chi connect toi system khi connect toi GW, khong connect truc tiep toi SV

# ghi chu
ptr -> mem[sd]
retr -> up? undef -> cant read -> rst 000 -> ned rstart -> read again
ptr rst-> mem[data-mem-sd after sd-header] 
ptr -> head + offset

# To do
- Bổ sung thêm ngày update gần nhất của ESP32 trong request -> GW so sánh với ngày update của FW trên GW để chọn FW phù hợp nhất
- Bổ sung việc đăng ký FW trên SV để GW có thể chuyển tiếp FW từ SV đến ESP32 khi không tìm thấy trên GW
- Kiểm tra và xử lý các trường hợp lỗi trong quá trình tải và cập nhật FW